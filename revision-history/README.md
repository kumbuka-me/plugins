# Revision History

Revision History adds the latest revision summary to page details and opens Kumbuka's full revision-history fragment in a host dialog.

Kumbuka core continues to own revision persistence, restore authorization, diffs, and the full history endpoint. The plugin owns the optional page-details presentation.

## Permissions

- `pages:read` reads authorized public revision metadata for the current page.
